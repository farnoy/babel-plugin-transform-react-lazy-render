{
  "name": "babel-plugin-transform-react-lazy-render",
  "version": "1.0.3",
  "description": "",
  "main": "index.compiled.js",
  "scripts": {
    "build": "yarn run babel LazyRender.source.js -o LazyRender.js && yarn run babel index.js -o index.compiled.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "keywords": [
    "babel-plugin"
  ],
  "peerDependencies": {
    "lodash": "^4.9"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1"
  }
}
